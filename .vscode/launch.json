{
  "configurations": [
    {
      "command": "yarn offline",
      "name": "Run start offline",
      "request": "launch",
      "type": "node-terminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}